#include<stdio.h>
int main()
{
    int num;
    printf("");
    scanf("%d",&num);
    if(num/5 && num/11);
    printf("Divisible",num);
    else
    printf("Not Divisible");
    return 0;
}